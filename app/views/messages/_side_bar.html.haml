.side-bar
  .side-bar__user-name
    .top-items
      %p.top-items__name
        = current_user.name
      %p.top-items__icons
        = link_to new_group_path, class: "top-items__icons" do
          = icon('far', 'edit')
        = link_to edit_user_path(current_user) do
          = icon('fas', 'cog')

  .side-bar__group-list
    - current_user.groups.each do |group|
      .Group
        = link_to '#' do
          %p.side-bar__group-name 
            = group.name
          %p.side-bar__message 
            メッセージはまだありません



 
